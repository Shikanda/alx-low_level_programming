#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
*strtow - a pointer to an array of strings
*@str: array of strings
*Return: NULL or pointer to a new string
*/
char **strtow(char *str)
{

	if (str == NULL || str == "")
		return (NULL);

	return (strtow);
}
